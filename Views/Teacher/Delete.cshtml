@model SchoolMangement.Models.techear

@{
    ViewBag.Title = "Delete";
}

<h2 class="text-capitalize text-primary text-center" style="margin-top:30px"> Delete Teacher </h2>

<hr style="background-color:black;width:600px;justify-content:center" />
<div class="container">
    <h3>Are you sure you want to delete this?</h3>
    
        
        <dl class="dl-horizontal">
            <dt>
                <label style="margin-bottom:10px; margin-left:0px;font-weight:bolder">Teacher Name</label>
            </dt>

            <dd>
                @Html.DisplayFor(model => model.name)
            </dd>

            <dt>
                <label style="margin-bottom:10px; margin-left:0px;font-weight:bolder">Course Name</label>
            </dt>

            <dd>
                @Html.DisplayFor(model => model.course.course1)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-danger" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
